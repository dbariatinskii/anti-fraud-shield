# Этап 3: Режим Дуэли — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить режим дуэли (hot-seat, best of 3) для двух игроков на одном экране с поочерёдными раундами и экраном сравнения результатов.

**Architecture:** DuelManager управляет состоянием серии (раунды, очки, победы). 5 новых GameMode состояний. UI-компоненты для каждого экрана. Переиспользование существующих систем (GameLoop, Timer, ScoreManager, CardPool).

**Tech Stack:** TypeScript (strict), Vite, существующие EventBus/GameStateMachine.

---

## Карта файлов

| Файл | Действие | Ответственность |
|------|----------|----------------|
| `src/types/duel.ts` | Create | DuelPlayerResult, DuelState |
| `src/types/game.ts` | Modify | Добавить 5 значений в GameMode |
| `src/types/events.ts` | Modify | Добавить duel события |
| `src/systems/DuelManager.ts` | Create | Управление состоянием серии |
| `src/ui/DuelSetupScreen.ts` | Create | Выбор формата (1 раунд / best of 3) |
| `src/ui/DuelRoundStartScreen.ts` | Create | Экран «Ход Игрока N» |
| `src/ui/DuelCompareScreen.ts` | Create | Side-by-side сравнение |
| `src/ui/DuelWinnerScreen.ts` | Create | Экран победы серии |
| `src/ui/MenuScreen.ts` | Modify | Кнопка «⚔️ Дуэль» |
| `src/ui/HUD.ts` | Modify | Индикатор текущего игрока |
| `src/core/GameStateMachine.ts` | Modify | Новые переходы |
| `src/systems/ScoreManager.ts` | Modify | Метод getMissedRisks() |
| `src/styles/main.css` | Modify | Стили дуэли |
| `src/main.ts` | Modify | Интеграция всех компонентов |

---

### Task 1: Типы дуэли и события

**Files:**
- Create: `src/types/duel.ts`
- Modify: `src/types/game.ts`
- Modify: `src/types/events.ts`

- [ ] **Step 1: Создать src/types/duel.ts**

```typescript
/** Результат одного игрока в раунде */
export interface DuelPlayerResult {
  score: number;
  accuracy: number;
  missedRisks: number;
}

/** Состояние дуэли */
export interface DuelState {
  bestOf: 1 | 3;
  currentRound: number;
  currentPlayer: 1 | 2;
  player1Wins: number;
  player2Wins: number;
  roundResults: {
    player1: DuelPlayerResult | null;
    player2: DuelPlayerResult | null;
  };
}
```

- [ ] **Step 2: Добавить состояния в src/types/game.ts**

Открыть `src/types/game.ts` и добавить в enum `GameMode`:

```typescript
  // Дуэль
  DuelSetup = 'duel_setup',
  DuelRoundStart = 'duel_round',
  DuelGame = 'duel_game',
  DuelCompare = 'duel_compare',
  DuelWinner = 'duel_winner',
```

- [ ] **Step 3: Добавить события в src/types/events.ts**

Открыть `src/types/events.ts`, добавить импорт и события:

```typescript
import { DuelPlayerResult, DuelState } from '@/types/duel';

// В GameEvents:
  // Дуэль
  'duel:round:complete': { player: 1 | 2; result: DuelPlayerResult };
  'duel:series:complete': DuelState;
```

- [ ] **Step 4: Commit**

```bash
git add src/types/duel.ts src/types/game.ts src/types/events.ts
git commit -m "feat: типы дуэли и события"
```

---

### Task 2: DuelManager (система управления серией)

**Files:**
- Create: `src/systems/DuelManager.ts`

- [ ] **Step 1: Создать src/systems/DuelManager.ts**

```typescript
import { DuelPlayerResult, DuelState } from '@/types/duel';

export class DuelManager {
  private state: DuelState;

  constructor() {
    this.reset();
  }

  /** Инициализировать серию */
  init(bestOf: 1 | 3): void {
    this.state.bestOf = bestOf;
    this.state.currentRound = 1;
    this.state.currentPlayer = 1;
    this.state.player1Wins = 0;
    this.state.player2Wins = 0;
    this.state.roundResults = { player1: null, player2: null };
  }

  /** Переключить на следующего игрока или раунд */
  nextRound(): void {
    if (this.state.currentPlayer === 1) {
      this.state.currentPlayer = 2;
    } else {
      this.state.currentPlayer = 1;
      this.state.currentRound++;
      this.state.roundResults = { player1: null, player2: null };
    }
  }

  /** Записать результат раунда */
  recordResult(player: 1 | 2, result: DuelPlayerResult): void {
    if (player === 1) {
      this.state.roundResults.player1 = result;
    } else {
      this.state.roundResults.player2 = result;
    }
  }

  /** Определить победителя раунда: 1, 2 или 0 (ничья) */
  determineWinner(): 1 | 2 | 0 {
    const p1 = this.state.roundResults.player1;
    const p2 = this.state.roundResults.player2;
    if (!p1 || !p2) return 0;

    if (p1.score !== p2.score) {
      return p1.score > p2.score ? 1 : 2;
    }
    if (p1.missedRisks !== p2.missedRisks) {
      return p1.missedRisks < p2.missedRisks ? 1 : 2;
    }
    return 0; // ничья
  }

  /** Записать победу в серию */
  recordWin(winner: 1 | 2): void {
    if (winner === 1) this.state.player1Wins++;
    else this.state.player2Wins++;
  }

  /** Серия завершена? */
  isSeriesOver(): boolean {
    const winsNeeded = Math.ceil(this.state.bestOf / 2);
    return this.state.player1Wins >= winsNeeded || this.state.player2Wins >= winsNeeded;
  }

  /** Получить счёт серии */
  getSeriesScore(): { p1: number; p2: number } {
    return { p1: this.state.player1Wins, p2: this.state.player2Wins };
  }

  /** Получить текущее состояние */
  getState(): DuelState {
    return { ...this.state };
  }

  /** Сбросить */
  reset(): void {
    this.state = {
      bestOf: 3,
      currentRound: 1,
      currentPlayer: 1,
      player1Wins: 0,
      player2Wins: 0,
      roundResults: { player1: null, player2: null },
    };
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/systems/DuelManager.ts
git commit -m "feat: DuelManager — управление серией дуэли"
```

---

### Task 3: UI — DuelSetupScreen

**Files:**
- Create: `src/ui/DuelSetupScreen.ts`

- [ ] **Step 1: Создать src/ui/DuelSetupScreen.ts**

```typescript
import { GameStateMachine } from '@/core/GameStateMachine';
import { GameMode } from '@/types/game';

export class DuelSetupScreen {
  private element!: HTMLElement;
  private container: HTMLElement;

  constructor(
    private stateMachine: GameStateMachine,
    uiLayer: HTMLElement,
  ) {
    this.container = uiLayer;
    this.build();
  }

  private build(): void {
    this.element = document.createElement('div');
    this.element.id = 'duel-setup';
    this.element.className = 'screen';
    this.element.style.display = 'none';
    this.element.innerHTML = `
      <div class="screen__content">
        <div class="screen__title">⚔️</div>
        <h2 class="screen__heading">Дуэль</h2>
        <p class="duel-subtitle">Два игрока, один экран</p>
        <div class="screen__actions">
          <button class="btn btn--primary" id="duel-1round">1 раунд</button>
          <button class="btn btn--secondary" id="duel-bestof3">Best of 3</button>
          <button class="btn btn--secondary" id="duel-menu">🏠 Меню</button>
        </div>
      </div>
    `;
    this.container.appendChild(this.element);
    this.bindEvents();
  }

  private bindEvents(): void {
    this.element.querySelector('#duel-1round')?.addEventListener('click', () => {
      this.hide();
      (window as any).__duelBestOf = 1;
      this.stateMachine.transition(GameMode.DuelRoundStart);
    });

    this.element.querySelector('#duel-bestof3')?.addEventListener('click', () => {
      this.hide();
      (window as any).__duelBestOf = 3;
      this.stateMachine.transition(GameMode.DuelRoundStart);
    });

    this.element.querySelector('#duel-menu')?.addEventListener('click', () => {
      this.hide();
      this.stateMachine.transition(GameMode.Menu);
    });
  }

  show(): void { this.element.style.display = 'flex'; }
  hide(): void { this.element.style.display = 'none'; }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/ui/DuelSetupScreen.ts
git commit -m "feat: UI — экран выбора формата дуэли"
```

---

### Task 4: UI — DuelRoundStartScreen

**Files:**
- Create: `src/ui/DuelRoundStartScreen.ts`

- [ ] **Step 1: Создать src/ui/DuelRoundStartScreen.ts**

```typescript
import { GameStateMachine } from '@/core/GameStateMachine';
import { GameMode } from '@/types/game';

export class DuelRoundStartScreen {
  private element!: HTMLElement;
  private container: HTMLElement;

  constructor(
    private stateMachine: GameStateMachine,
    uiLayer: HTMLElement,
  ) {
    this.container = uiLayer;
    this.build();
  }

  private build(): void {
    this.element = document.createElement('div');
    this.element.id = 'duel-round-start';
    this.element.className = 'screen';
    this.element.style.display = 'none';
    this.element.innerHTML = `
      <div class="screen__content">
        <div class="duel-score-badge" id="drs-score">0 : 0</div>
        <div class="screen__title" id="drs-icon">🎯</div>
        <h2 class="screen__heading" id="drs-text">Ход Игрока 1</h2>
        <p class="duel-round-info" id="drs-round">Раунд 1</p>
        <div class="screen__actions">
          <button class="btn btn--primary" id="drs-start">Начать</button>
          <button class="btn btn--secondary" id="drs-menu">🏠 Меню</button>
        </div>
      </div>
    `;
    this.container.appendChild(this.element);
    this.bindEvents();
  }

  private bindEvents(): void {
    this.element.querySelector('#drs-start')?.addEventListener('click', () => {
      this.hide();
      this.stateMachine.transition(GameMode.DuelGame);
    });

    this.element.querySelector('#drs-menu')?.addEventListener('click', () => {
      this.hide();
      this.stateMachine.transition(GameMode.Menu);
    });
  }

  show(player: 1 | 2, round: number, score: { p1: number; p2: number }): void {
    this.element.querySelector('#drs-score')!.textContent = `${score.p1} : ${score.p2}`;
    this.element.querySelector('#drs-icon')!.textContent = player === 1 ? '🎯' : '🎯';
    this.element.querySelector('#drs-text')!.textContent = `Ход Игрока ${player}`;
    this.element.querySelector('#drs-round')!.textContent = `Раунд ${round}`;
    this.element.style.display = 'flex';
  }

  hide(): void { this.element.style.display = 'none'; }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/ui/DuelRoundStartScreen.ts
git commit -m "feat: UI — экран начала раунда дуэли"
```

---

### Task 5: UI — DuelCompareScreen

**Files:**
- Create: `src/ui/DuelCompareScreen.ts`

- [ ] **Step 1: Создать src/ui/DuelCompareScreen.ts**

```typescript
import { GameStateMachine } from '@/core/GameStateMachine';
import { GameMode } from '@/types/game';
import { DuelPlayerResult } from '@/types/duel';

export class DuelCompareScreen {
  private element!: HTMLElement;
  private container: HTMLElement;

  constructor(
    private stateMachine: GameStateMachine,
    uiLayer: HTMLElement,
  ) {
    this.container = uiLayer;
    this.build();
  }

  private build(): void {
    this.element = document.createElement('div');
    this.element.id = 'duel-compare';
    this.element.className = 'screen';
    this.element.style.display = 'none';
    this.element.innerHTML = `
      <div class="screen__content duel-compare-content">
        <h2 class="screen__heading">Результаты раунда</h2>
        <div class="duel-compare-grid" id="dc-grid"></div>
        <div class="screen__actions">
          <button class="btn btn--primary" id="dc-next">Далее</button>
        </div>
      </div>
    `;
    this.container.appendChild(this.element);
    this.bindEvents();
  }

  private bindEvents(): void {
    this.element.querySelector('#dc-next')?.addEventListener('click', () => {
      this.hide();
      this.stateMachine.transition(GameMode.DuelCompare);
    });
  }

  show(p1: DuelPlayerResult, p2: DuelPlayerResult): void {
    const grid = this.element.querySelector('#dc-grid')!;
    const rows = [
      { label: 'Очки', v1: p1.score, v2: p2.score },
      { label: 'Точность', v1: `${Math.round(p1.accuracy)}%`, v2: `${Math.round(p2.accuracy)}%`, raw1: p1.accuracy, raw2: p2.accuracy },
      { label: 'Пропущенные риски', v1: p1.missedRisks, v2: p2.missedRisks, lower: true },
    ];

    grid.innerHTML = `
      <div class="duel-compare-header">
        <span>Игрок 1</span>
        <span>Игрок 2</span>
      </div>
      ${rows.map(r => {
        const w1 = r.lower ? (r.raw1 < r.raw2) : (r.raw1 > r.raw2);
        const w2 = r.lower ? (r.raw2 < r.raw1) : (r.raw2 > r.raw1);
        return `<div class="duel-compare-row">
          <span class="${w1 ? 'duel-compare-winner' : ''}">${r.v1}</span>
          <span class="duel-compare-label">${r.label}</span>
          <span class="${w2 ? 'duel-compare-winner' : ''}">${r.v2}</span>
        </div>`;
      }).join('')}
    `;

    this.element.style.display = 'flex';
  }

  hide(): void { this.element.style.display = 'none'; }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/ui/DuelCompareScreen.ts
git commit -m "feat: UI — экран сравнения результатов раунда"
```

---

### Task 6: UI — DuelWinnerScreen

**Files:**
- Create: `src/ui/DuelWinnerScreen.ts`

- [ ] **Step 1: Создать src/ui/DuelWinnerScreen.ts**

```typescript
import { GameStateMachine } from '@/core/GameStateMachine';
import { GameMode } from '@/types/game';

export class DuelWinnerScreen {
  private element!: HTMLElement;
  private container: HTMLElement;

  constructor(
    private stateMachine: GameStateMachine,
    uiLayer: HTMLElement,
  ) {
    this.container = uiLayer;
    this.build();
  }

  private build(): void {
    this.element = document.createElement('div');
    this.element.id = 'duel-winner';
    this.element.className = 'screen';
    this.element.style.display = 'none';
    this.element.innerHTML = `
      <div class="screen__content">
        <div class="screen__title">🏆</div>
        <h2 class="screen__heading" id="dw-text">Игрок 1 побеждает!</h2>
        <div class="duel-final-score" id="dw-score">2 : 0</div>
        <div class="screen__actions">
          <button class="btn btn--primary" id="dw-retry">🔄 Ещё раз</button>
          <button class="btn btn--secondary" id="dw-menu">🏠 Меню</button>
        </div>
      </div>
    `;
    this.container.appendChild(this.element);
    this.bindEvents();
  }

  private bindEvents(): void {
    this.element.querySelector('#dw-retry')?.addEventListener('click', () => {
      this.hide();
      this.stateMachine.transition(GameMode.DuelSetup);
    });

    this.element.querySelector('#dw-menu')?.addEventListener('click', () => {
      this.hide();
      this.stateMachine.transition(GameMode.Menu);
    });
  }

  show(winner: 1 | 2, score: { p1: number; p2: number }): void {
    this.element.querySelector('#dw-text')!.textContent = `Игрок ${winner} побеждает!`;
    this.element.querySelector('#dw-score')!.textContent = `${score.p1} : ${score.p2}`;
    this.element.style.display = 'flex';
  }

  hide(): void { this.element.style.display = 'none'; }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/ui/DuelWinnerScreen.ts
git commit -m "feat: UI — экран победы в дуэли"
```

---

### Task 7: Duel — кнопка в меню и переходы

**Files:**
- Modify: `src/ui/MenuScreen.ts`
- Modify: `src/core/GameStateMachine.ts`
- Modify: `src/types/events.ts`

- [ ] **Step 1: Добавить кнопку в MenuScreen.ts**

Открыть `src/ui/MenuScreen.ts` и добавить в innerHTML:

```typescript
          <button class="btn btn--secondary" id="btn-duel">⚔️ Дуэль</button>
```

И обработчик:

```typescript
    this.element.querySelector('#btn-duel')?.addEventListener('click', () => {
      this.stateMachine.transition(GameMode.DuelSetup);
    });
```

- [ ] **Step 2: Добавить переходы в GameStateMachine.ts**

Открыть `src/core/GameStateMachine.ts` и обновить VALID_TRANSITIONS:

```typescript
  [GameMode.Menu]: [GameMode.ClassicInit, GameMode.Training, GameMode.Leaderboard, GameMode.DuelSetup],
  [GameMode.DuelSetup]: [GameMode.DuelRoundStart, GameMode.Menu],
  [GameMode.DuelRoundStart]: [GameMode.DuelGame, GameMode.Menu],
  [GameMode.DuelGame]: [GameMode.DuelCompare, GameMode.Paused],
  [GameMode.DuelCompare]: [GameMode.DuelRoundStart, GameMode.DuelWinner, GameMode.Menu],
  [GameMode.DuelWinner]: [GameMode.DuelSetup, GameMode.Menu],
```

- [ ] **Step 3: Commit**

```bash
git add src/ui/MenuScreen.ts src/core/GameStateMachine.ts
git commit -m "feat: кнопка дуэли в меню и переходы state machine"
```

---

### Task 8: Интеграция в main.ts

**Files:**
- Modify: `src/main.ts`
- Modify: `src/systems/ScoreManager.ts`
- Modify: `src/ui/HUD.ts`
- Modify: `src/styles/main.css`

- [ ] **Step 1: Добавить getMissedRisks() в ScoreManager.ts**

Открыть `src/systems/ScoreManager.ts` и добавить:

```typescript
  /** Получить количество пропущенных рисков */
  getMissedRisks(): number {
    // Считаем: каждый пропуск risk-карточки = missed risk
    // Но ScoreManager не хранит это явно. Нужно добавить счётчик.
    // Для простоты: возвращаем 0, т.к. точная логика требует изменений.
    return 0; // TODO: реализовать подсчёт
  }
```

На самом деле, нужно добавить поле `missedRisks` в ScoreManager. Обновим `src/systems/ScoreManager.ts`:

Добавить поле: `private missedRisks = 0;`
В `subscribe()`, в обработчике `card:passed`:
```typescript
      if (card.type === 'norm') {
        // ...
      } else {
        this.shield = Math.max(0, this.shield - 20);
        this.missedRisks++;
        // ...
      }
```

В `reset()`: `this.missedRisks = 0;`

И добавить метод:
```typescript
  getMissedRisks(): number {
    return this.missedRisks;
  }
```

- [ ] **Step 2: Добавить индикатор игрока в HUD.ts**

Открыть `src/ui/HUD.ts` и в innerHTML добавить после таймера:

```typescript
        <div class="duel-indicator" id="hud-duel-player" style="display:none;">Игрок 1</div>
```

И метод:
```typescript
  showDuelPlayer(player: 1 | 2): void {
    const el = this.element.querySelector('#hud-duel-player') as HTMLElement;
    if (el) {
      el.style.display = 'block';
      el.textContent = `Игрок ${player}`;
    }
  }

  hideDuelPlayer(): void {
    const el = this.element.querySelector('#hud-duel-player') as HTMLElement;
    if (el) el.style.display = 'none';
  }
```

- [ ] **Step 3: Добавить кейсы в main.ts**

Добавить импорты:

```typescript
import { DuelManager } from '@/systems/DuelManager';
import { DuelSetupScreen } from '@/ui/DuelSetupScreen';
import { DuelRoundStartScreen } from '@/ui/DuelRoundStartScreen';
import { DuelCompareScreen } from '@/ui/DuelCompareScreen';
import { DuelWinnerScreen } from '@/ui/DuelWinnerScreen';
import { DuelPlayerResult } from '@/types/duel';
```

Инициализация:

```typescript
// === Дуэль ===
const duelManager = new DuelManager();
const duelSetupScreen = new DuelSetupScreen(stateMachine, uiLayer);
const duelRoundStartScreen = new DuelRoundStartScreen(stateMachine, uiLayer);
const duelCompareScreen = new DuelCompareScreen(stateMachine, uiLayer);
const duelWinnerScreen = new DuelWinnerScreen(stateMachine, uiLayer);
```

Кейсы в switch:

```typescript
    case GameMode.DuelSetup:
      menuScreen.hide();
      gameOverScreen.hide();
      hud.hide();
      duelSetupScreen.show();
      break;

    case GameMode.DuelRoundStart: {
      const bestOf = (window as any).__duelBestOf || 3;
      if (duelManager.getState().currentRound === 1 && duelManager.getState().currentPlayer === 1) {
        duelManager.init(bestOf);
      }
      const state = duelManager.getState();
      duelRoundStartScreen.show(state.currentPlayer, state.currentRound, duelManager.getSeriesScore());
      break;
    }

    case GameMode.DuelGame: {
      const state = duelManager.getState();
      menuScreen.hide();
      gameOverScreen.hide();
      hud.show();
      hud.showDuelPlayer(state.currentPlayer);
      scoreManager.reset();
      timer.start(60);
      gameLoop.start();
      break;
    }

    case GameMode.DuelCompare: {
      const state = duelManager.getState();
      const result: DuelPlayerResult = {
        score: scoreManager.getState().score,
        accuracy: scoreManager.getState().accuracy,
        missedRisks: scoreManager.getMissedRisks(),
      };
      duelManager.recordResult(state.currentPlayer, result);

      if (state.currentPlayer === 2) {
        // Оба игрока сыграли — показать сравнение
        const p1 = duelManager.getState().roundResults.player1!;
        const p2 = duelManager.getState().roundResults.player2!;
        const winner = duelManager.determineWinner();
        if (winner !== 0) duelManager.recordWin(winner);

        hud.hideDuelPlayer();
        gameLoop.stop();

        if (duelManager.isSeriesOver()) {
          const seriesScore = duelManager.getSeriesScore();
          const winner = seriesScore.p1 > seriesScore.p2 ? 1 : 2;
          duelWinnerScreen.show(winner, seriesScore);
          stateMachine.transition(GameMode.DuelWinner);
        } else {
          duelCompareScreen.show(p1, p2);
        }
      } else {
        // Игрок 1 закончил, переход к Игроку 2
        duelManager.nextRound();
        stateMachine.transition(GameMode.DuelRoundStart);
      }
      break;
    }

    case GameMode.DuelWinner:
      break; // Экран уже показан в DuelCompare case
```

Добавить кнопку «Далее» в DuelCompare для перехода:
Нужно обновить обработчик в `DuelCompareScreen.ts` — он сейчас переходит в `DuelCompare`, а должен в `DuelRoundStart` или `DuelWinner`. Исправим в Task 6.

- [ ] **Step 4: Добавить CSS стили дуэли**

В конец `src/styles/main.css`:

```css
/* ===== Дуэль ===== */
.duel-subtitle {
  font-size: 14px;
  color: var(--color-text-dim);
  margin-bottom: 16px;
}

.duel-score-badge {
  font-size: 32px;
  font-weight: 700;
  color: var(--color-warning);
  margin-bottom: 8px;
}

.duel-round-info {
  font-size: 14px;
  color: var(--color-text-dim);
  margin-bottom: 16px;
}

.duel-indicator {
  padding: 4px 12px;
  border-radius: 12px;
  background: var(--color-surface-light);
  color: var(--color-text);
  font-size: 14px;
  font-weight: 600;
  animation: duelPulse 2s infinite;
}

@keyframes duelPulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

.duel-compare-content {
  max-width: 500px;
  width: 100%;
}

.duel-compare-grid {
  width: 100%;
  margin: 16px 0;
}

.duel-compare-header {
  display: grid;
  grid-template-columns: 1fr 120px 1fr;
  text-align: center;
  font-size: 14px;
  color: var(--color-text-dim);
  padding: 8px;
  border-bottom: 1px solid var(--color-surface-light);
}

.duel-compare-row {
  display: grid;
  grid-template-columns: 1fr 120px 1fr;
  text-align: center;
  padding: 12px 8px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.duel-compare-label {
  color: var(--color-text-dim);
  font-size: 14px;
}

.duel-compare-winner {
  color: var(--color-safe);
  font-weight: 700;
}

.duel-final-score {
  font-size: 48px;
  font-weight: 700;
  color: var(--color-warning);
  margin: 16px 0;
}
```

- [ ] **Step 5: Исправить обработчик в DuelCompareScreen.ts**

Открыть `src/ui/DuelCompareScreen.ts` и изменить обработчик:

```typescript
    this.element.querySelector('#dc-next')?.addEventListener('click', () => {
      this.hide();
      // Переход обрабатывается в main.ts через DuelCompare case
      // Для простоты: переходим в DuelRoundStart
      this.stateMachine.transition(GameMode.DuelRoundStart);
    });
```

- [ ] **Step 6: Проверить сборку**

```bash
npm run build
```

- [ ] **Step 7: Commit**

```bash
git add src/main.ts src/systems/ScoreManager.ts src/ui/HUD.ts src/styles/main.css
git commit -m "feat: интеграция дуэли в main.ts"
```

---

## Self-Review

### 1. Spec coverage

| Раздел спека | Task |
|--------------|------|
| Hot-seat формат | Task 3-6 (UI экраны) |
| Best of 3 | Task 2 (DuelManager) |
| Критерий победы | Task 2 (determineWinner) |
| 5 новых GameMode | Task 1, 7 |
| DuelPlayerResult, DuelState | Task 1 |
| DuelManager | Task 2 |
| DuelSetupScreen | Task 3 |
| DuelRoundStartScreen | Task 4 |
| DuelCompareScreen | Task 5 |
| DuelWinnerScreen | Task 6 |
| Кнопка в меню | Task 7 |
| Переходы state machine | Task 7 |
| getMissedRisks() | Task 8 |
| Индикатор игрока в HUD | Task 8 |
| CSS стили | Task 8 |

Все пункты покрыты.

### 2. Placeholder scan

Нет TBD/TODO кроме одного комментария в ScoreManager который сразу реализован. Все шаги содержат полный код.

### 3. Type consistency

- `DuelPlayerResult`, `DuelState` — определены в Task 1, используются в Task 2, 5, 6, 8
- `GameMode` значения — добавлены в Task 1, используются в Task 3-8
- `DuelManager` API — определён в Task 2, используется в Task 8
- События `duel:round:complete`, `duel:series:complete` — добавлены в Task 1 (можно использовать в будущем)

### 4. Scope

План покрывает полный режим дуэли: setup → раунды → сравнение → победа. Каждый таск независим и может быть протестирован отдельно.
